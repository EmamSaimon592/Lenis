const lenis = new Lenis({
  wrapper: document.getElementById('lenis-wrapper'),
})


lenis.on('scroll', (e) => {
  console.log(e)
})

function raf(time) {
  lenis.raf(time)
  requestAnimationFrame(raf)
}

requestAnimationFrame(raf)

